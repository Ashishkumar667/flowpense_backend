import prisma from '../../config/db.js';
import asyncHandler from 'express-async-handler';

export const notificationController = asyncHandler(async(req, res) => {
    try {
        
    } catch (error) {
        
    }
})